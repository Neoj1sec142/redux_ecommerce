// HASH FUNCTIONS FOR USER CARD INFO HERE
const formatCheckout = () => {
    return
}
const hashCard = (cardInfo) => {
    // const cardNumber = cardInfo.card_num
    // hashed_card, hased_cv, hashed_exp, (FIELDS) 
}